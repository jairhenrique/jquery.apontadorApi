#Plugin jQuery para a API do Apontador
<http://api.apontador.com.br>

##Modo de usar:

`var api = new apontadorApi('sua_consumer_key');`
`api.getPlaces({'q' : 'Uziel' }, function(data){`
    `console.log(data);`
`});`
